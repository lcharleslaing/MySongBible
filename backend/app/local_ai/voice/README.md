# Voice Orchestration

This folder is intended for shared voice-layer orchestration, contracts, and common types that need to be shared across STT, TTS, and future cloning features.

Use this layer when logic would otherwise be duplicated between `stt/`, `tts/`, and `voice_cloning/`.
