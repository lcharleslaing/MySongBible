# Shared Layer

This directory holds configuration references and future cross-layer contracts that should not live exclusively in Electron, frontend, or backend.

Today, most active runtime configuration still lives inside the backend settings layer so SQLite-backed overrides can remain authoritative.
