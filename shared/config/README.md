# Shared Configuration

This folder is intended for shared configuration references, defaults, and future typed config contracts when a setting must be understood by multiple layers without duplicating semantics.

`device-profiles.json` is created by Settings when you save Device Profiles. It stores named per-machine Whisper, Piper, TTS, and audio path bundles that can be committed when you want those profiles available after cloning on another device.
