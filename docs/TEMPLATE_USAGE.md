# Template Usage

The canonical template reuse guide lives at [`../TEMPLATE_USAGE.md`](../TEMPLATE_USAGE.md).

Use that guide when cloning AppTemplateBase into a new app, renaming metadata, choosing a SQLite filename, and keeping local runtime data out of Git.
